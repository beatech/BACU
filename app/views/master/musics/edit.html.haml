= link_to '戻る', master_musics_path

%h2 マスター部門課題曲2基準点
= form_tag master_music_path, method: 'PATCH' do
  %table.table.table-bordered.table-striped
    %thead
      %tr
        %td 機種
        %td{ colspan: 3 } 課題曲その2（KACに被せる）
    %tbody
      - Game.all.each do |game|
        %tr
          %td= game.title
          - game.master_musics.where(music_order: 2).each do |music|
            %td= music.difficulty
            %td= music.title
            %td= text_field_tag "insurance[#{music.id}]", music.insurance_score || 0, class: 'span4'
  = submit_tag '曲目を更新', class: 'btn btn-primary'
