.entries
  %h3
    ページ一覧
    = link_to 'ページを新規作成する', new_entry_path, class: 'btn btn-primary'
  %p 計 #{Entry.all.count} ページ
  %ul
    - Entry.all.each do |entry|
      %li
        = link_to entry.title, entry_path(entry)
        = link_to '編集', edit_entry_path(entry), class: 'btn'
