= link_to '戻る', master_musics_path

%h2 マスター部門課題曲
= form_tag master_music_path, method: 'PATCH' do
  %table.table.table-bordered.table-striped
    %thead
      %tr
        %td 機種
        %td{ colspan: 2 } 課題曲その1（KAC関係なく簡単なもの）
        %td{ colspan: 2 } 課題曲その2（KACに被せる）
    %tbody
      - Game.all.each do |game|
        %tr
          %td= game.title
          - game.master_musics.each do |music|
            %td= text_field_tag "difficulty[#{music.id}]", music.difficulty, class: 'span1'
            %td= text_field_tag "title[#{music.id}]", music.title, class: 'span4'
  = submit_tag '曲目を更新', class: 'btn btn-primary'
