= link_to 'ホームに戻る', root_url

%h1 サークル一覧
%p サークル得点計算は現在調整中です。ご迷惑をお掛けいたします。

#circles
  %table.table.table-bordered.table-striped
    %thead
      %tr
        %td 順位
        %td サークル
        %td 大学
        %td 得点
        %td 参加人数
    %tbody
      - last_score = 0
      - last_rank = 1
      - @circles.each_with_index do |master_circle, index|
        - unless last_score == master_circle.total_score
          - last_score = master_circle.total_score
          - last_rank = index + 1
        %tr
          %td= last_rank
          %td= link_to master_circle.name, circle_path(master_circle.circle)
          %td= master_circle.circle.university
          %td= master_circle.total_score
          %td= master_circle.circle.users.count
